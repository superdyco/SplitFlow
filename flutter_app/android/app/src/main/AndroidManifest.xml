<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 收據拍照。image_picker 走的是系統相機 App，所以不需要 CAMERA 權限
         （那是自己開相機預覽才要的）；相簿選圖走的是系統選擇器，同樣不需要
         READ_MEDIA_IMAGES。宣告 android.hardware.camera 但 required=false，
         沒有相機的裝置照樣裝得起來，只是拍照那個選項按了沒反應。 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 「定位」鍵：抓現在的座標當作地點搜尋的位置偏好。
         只要粗略位置就夠了（偏好半徑是 30km），但 Places 的
         geolocator 在 Android 上兩個都會問，所以兩個都宣告。
         這是**選填功能**，使用者拒絕權限只是少了那顆鍵的效果。 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Android 13 起通知是 runtime permission。宣告在這裡只是讓系統認得，
         真正要不要跳對話框由程式決定 —— 見 PushRepository.requestPermission。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="splitflow"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- 值來自 android/local.properties 的 MAPS_API_KEY（不進版控）。
             沒設的話是空字串，地圖會是灰的 —— 所以 Dart 那邊也拿得到同一個
             資訊，沒設就不畫地圖、改顯示說明。 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
