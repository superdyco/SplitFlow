# 複製成 .env 再填入真實值。.env 不進版控。
#
# Firebase 的這幾個值在 Firebase Console → 專案設定 → 一般 → 你的應用程式 找得到。
# 它們會被打包進前端原始碼，本來就是公開的：真正的防線是 Firestore rules
# 與 Authentication 的 Authorized domains，不是把這些值藏起來。

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# 以下兩把都是選填，而且分開管理，可以各自設限制、各自更換。
# 跟 Firebase 那把不同，這兩把是「用量計費」的，被別人撿去用會直接算你的帳單。
# 務必在 Google Cloud Console 設定：
#   1. 應用程式限制 → 網站：正式網域與 http://localhost:5173/*
#   2. API 限制：各自只勾用得到的那一個 API
#   3. 配額上限與預算警示 —— referrer header 可以偽造，這才是財務防線

# Places API (New)。沒設的話支出的地點欄位會退回純文字輸入。
VITE_GOOGLE_PLACES_API_KEY=

# 地圖。沒設的話不會顯示地圖，其餘功能不受影響。
#
# 這把 key 要開通「兩個」API，它們在 Google Cloud 是分開的：
#   - Maps JavaScript API：支出頁的互動地圖
#   - Maps Static API：公開旅費報告裡那張地圖（產生報告時抓一次存起來）
# 只開通其中一個的話，另一邊會靜靜地變成「沒有地圖」。
# 所以上面「各自只勾用得到的那一個 API」對這把 key 是勾這兩個。
VITE_GOOGLE_MAPS_API_KEY=
